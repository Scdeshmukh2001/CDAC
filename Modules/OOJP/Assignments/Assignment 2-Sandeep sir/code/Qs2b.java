public class Qs2b  {
    public static void main(String[] args) {
        // Byte.BYTES is a constant holding the number of bytes used to represent a byte value
        int byteSize = Byte.BYTES;
        
        System.out.println("Number of bytes used to represent a byte value: " + byteSize);
    }
}
